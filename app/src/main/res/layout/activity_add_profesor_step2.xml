<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:padding="40dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Botón atrás -->
        <ImageButton
            android:id="@+id/btnBack"
            style="@style/BtnBack"
            android:contentDescription="@string/btn_back_description" />

        <!-- Título -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="@string/add_profesor"
            android:paddingHorizontal="35sp"
            style="@style/MainTitle"/>

    </RelativeLayout>

    <LinearLayout
        android:id="@+id/profesorOptContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:paddingTop="50dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <Spinner
            android:id="@+id/spinnerOptions"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:spinnerMode="dropdown"
            android:background="@color/desabledBg"
            android:padding="8sp"/>


        <LinearLayout
            android:id="@+id/ActivitySelectedContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:paddingTop="24dp"
            android:visibility="invisible">

            <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Seleccionaste:"
                    android:textColor="@color/green"
                    android:textSize="20sp" />

                <TextView
                    android:id="@+id/ActivitySelected"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Opción"
                    android:textColor="@color/green"
                    android:textSize="20sp"
                    android:paddingLeft="10sp"
                    android:textStyle="bold"/>
            </LinearLayout>

            <TableLayout
                android:id="@+id/timeTable"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="16dp">

                <!-- Encabezado -->
                <TableRow>
                    <TextView android:text="Días" style="@style/TableHeaderText"/>
                    <TextView android:text="Inicio" style="@style/TableHeaderText"/>
                    <TextView android:text="Fin" style="@style/TableHeaderText"/>
                </TableRow>

                <!-- Fila 1 -->
                <TableRow>
                    <TextView android:text="Lunes" style="@style/TableRowText"/>
                    <TextView android:text="17:00" style="@style/TableRowText"/>
                    <TextView android:text="20:00" style="@style/TableRowText"/>
                </TableRow>

                <!-- Fila 2 -->
                <TableRow>
                    <TextView android:text="Jueves" style="@style/TableRowText"/>
                    <TextView android:text="17:00" style="@style/TableRowText"/>
                    <TextView android:text="20:00" style="@style/TableRowText"/>
                </TableRow>
        </TableLayout>


        </LinearLayout>

    </LinearLayout>

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btnAddProfNext2"
        style="@style/ButtonPrimary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:enabled="false"
        android:text="Siguiente"
        android:textAllCaps="false" />

</LinearLayout>
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:padding="40dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageButton
            android:id="@+id/btnBack"
            style="@style/BtnBack"
            android:contentDescription="@string/btn_back_description" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="Registro de actividad"
            android:paddingHorizontal="50dp"
            style="@style/MainTitle"/>
    </RelativeLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingTop="16dp">

        <TableLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TableRow style="@style/TableRowContainer">
                <TextView android:text="Nombre" style="@style/TextDetail"/>
                <TextView android:id="@+id/tvName" style="@style/TextDetail"/>
            </TableRow>

            <TableRow style="@style/TableRowContainer">
                <TextView android:text="Día" style="@style/TextDetail"/>
                <TextView android:id="@+id/tvDays" style="@style/TextDetail"/>
            </TableRow>

            <TableRow style="@style/TableRowContainer">
                <TextView android:text="Horario" style="@style/TextDetail"/>
                <TextView android:id="@+id/tvTime" style="@style/TextDetail"/>
            </TableRow>

            <TableRow style="@style/TableRowContainer">
                <TextView android:text="Precio" style="@style/TextDetail"/>
                <TextView android:id="@+id/tvPrice" style="@style/TextDetail"/>
            </TableRow>

            <TableRow style="@style/TableRowContainer">
                <TextView android:text="Cupo" style="@style/TextDetail"/>
                <TextView android:id="@+id/tvCapacity" style="@style/TextDetail"/>
            </TableRow>

        </TableLayout>
    </LinearLayout>

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btnAddActSave"
        style="@style/ButtonPrimary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Guardar"
        android:textAllCaps="false" />
</LinearLayout>
